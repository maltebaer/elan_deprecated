import "./HorizontalBar.css";

import React from "react";

const HorizontalBar: React.FC = () => {
  return (
    <div className="HorizontalBar d-flex flex-column justify-content-center">
      <div className="w-100 bg-dark" />
    </div>
  );
};

export default HorizontalBar;
